import React from 'react';

interface IProps {
    sidebar?: any;
    children?: any;
}

const NoteListContainer:React.FC<IProps> = ({
    sidebar,
    children
}) => {

    return(
        <>

        </>
    );
}

export default NoteListContainer;
